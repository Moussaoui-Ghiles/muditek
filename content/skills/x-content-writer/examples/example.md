# Fictional example

Source inventory:

- one browser-side tool;
- five deterministic checks;
- no campaign result;
- no customer story.

Use a long post if each check needs explanation. Use a short post only when the piece teaches one check. Reject a thread that merely breaks the five checks into five thin posts.
